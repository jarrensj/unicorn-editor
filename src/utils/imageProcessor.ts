
// Re-export from the new structured location to maintain backward compatibility
import { processImageDownload, handleImageAction } from "./image/processor";

export { processImageDownload, handleImageAction };
